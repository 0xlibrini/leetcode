#include <stdio.h>
#include <stdio.h>

void    check_vowel(char s)
{
    if(s == 'a' || s == 'e' || s == 'i'  
        || s == 'o' || s == 'u' || s == 'A' || s == 'E' || s == 'I'  
        || s == 'O' || s == 'U' ||)

}
char* reverseVowels(char* s) {
    
}
int main()
{
    char *s = "IceCream";
    char *s = reverseVowels(s);
    printf("%s\n",s);
}